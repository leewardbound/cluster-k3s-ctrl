---
apiVersion: v1
kind: Service
metadata:
  name: &app adguard
  namespace: networking
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  type: ExternalName
  externalName: 10.20.30.1
  ports:
    - name: http
      port: &port 3010
      protocol: TCP
      targetPort: *port
