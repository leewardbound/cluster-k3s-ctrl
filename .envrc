#shellcheck disable=SC2148,SC2155
export KUBECONFIG=$(expand_path ~/.kube/clusters/ctrl)
export ANSIBLE_CONFIG=$(expand_path ./ansible.cfg)
export ANSIBLE_HOST_KEY_CHECKING="False"
export SOPS_AGE_KEY_FILE=$(expand_path ~/.secrets/infra/age-key.txt)

source .config.env

# Local config
[[ -f .config.env ]] && echo $blue"[LOADING] ./.config.env..." && source ./.config.env
