---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-mattermost-postgresql
  namespace: business
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: ceph-block
